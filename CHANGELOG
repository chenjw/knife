0.8.13
1. 添加find -hc hexhashcode功能，支持根据对象toString时打印的hash码来查找对象
2. 连接选择进程时原来只能输入进程序号，修改为还可以直接输入进程号

0.8.14
1. 支持perf命令，用于显示热点方法调用
2. 所有操作信息会写入主目录里的knife.log，每次重新启动后会清空这个日志

0.9.0
1. 支持perf命令修改为thread命令，能够通过采样的方式查看热点线程。
2. 增加heap命令，通过采样的方式查看内存占用和增量。
3. 修复带参数命令（类似heap -i -t 10）后面必须加个空格的bug
4. 支持持续刷新屏幕的命令（类似heap）

0.9.1
1. 添加heap命令中的gc和内存大小信息展示





